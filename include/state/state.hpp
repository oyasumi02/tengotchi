#pragma once

namespace State {
    enum class ScreenType {
        PET,
        FEED,
        LIGHTS,
        PLAY,
        DISCIPLINE,
        STATS,
        SICK,
        DEBUG,
        QUIT
    };
}